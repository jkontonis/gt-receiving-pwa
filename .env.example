# Neon / Vercel Postgres connection string (Sydney region to match qc-tracker)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# PIN that unlocks the Admin area + edit/delete/export. Change from the default.
ADMIN_PIN=0000
